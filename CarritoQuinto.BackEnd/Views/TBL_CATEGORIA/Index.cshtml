@model IEnumerable<CarritoQuinto.BackEnd.Models.TBL_CATEGORIA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cat_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_fechacreacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cat_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_fechacreacion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.cat_id }) |
            @Html.ActionLink("Detalle", "Details", new { id=item.cat_id }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.cat_id })
        </td>
    </tr>
}

</table>
